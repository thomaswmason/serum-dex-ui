import React from 'react';
import { Octokit } from '@octokit/rest';
import { useTokenInfo } from '../../utils/nfts/utils';
import { Row, Col, Form, Input, Checkbox } from 'antd';

// Need IPFS, name, mint, marketAddress supply

const ListingExplainer = () => {
  return null;
};

const ListingForm = () => {
  return null;
};

const Listing = () => {
  return null;
};

export default Listing;
